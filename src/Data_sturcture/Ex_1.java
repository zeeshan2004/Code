package Data_sturcture;

public class Ex_1 {
    /*Search and Sort

        #include <stdio.h>

            int main() {
                //  ***************** Sorted   *****************
                int array[100],a,b,i,j,swap,g;
                printf("Enter an Elements :");
                scanf("%d",&a);

                printf("Enter an %d numbers : " ,a);

        // for inter a Varibles
                for( i=0;i<a;i++){
                    scanf("%d",&array[i]);
                }

                for(g=0;g<a-1;g++){
                    for (int k =0;k<a-1;k++){
                        if(array[k]>array[k+1]){
                            swap = array[k];

                            array[k]=array[k+1];
                            array[k+1]=swap;
                        }}}
        // For print the Varibles :

                for( j = 0; j<a ; j++){
                    printf("%d ",array[j]);
                }

        //  ***************** for Search Pattrn *****************
                int y;
                int found =0;
                printf("\n Search the elemnt : ");
                scanf("%d",&y);

                for(int k =0;k<a;k++){
                    if(y==array[k]){
                        found= 1;
                        break ;
                    }
                }
                if(found){
                    printf("Found !");
                }

                else{
                    printf("Not Found !");
                }
                return 0;
            }*/
}
