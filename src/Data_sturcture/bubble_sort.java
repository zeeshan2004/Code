        package Data_sturcture;
        // In C++ and Bubble sort in Dynamic way
        public class bubble_sort {




            /*
            #include <stdio.h>

            int main() {
                int array[ 100],a, b, i, j, swap, g;
                printf("Enter an Elements :");
                scanf("%d", & a);

                printf("Enter an %d numbers : ", a);

        // for inter a Varibles
                for (i = 0; i < a; i++) {
                    scanf("%d", & array[i]);
                }

                for (g = 0; g < a - 1; g++) {
                    for (int k = 0; k < a - 1; k++) {
                        if (array[k] > array[k + 1]) {
                            swap = array[k];

                            array[k] = array[k + 1];
                            array[k + 1] = swap;
                        }
                    }
                }
        // For print the Varibles :


                for (j = 0; j < a; j++) {
                    printf("%d ", array[j]);
                }
                return 0;

            }*/
        }